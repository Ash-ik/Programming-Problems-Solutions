#include<stdio.h>
#include<string.h>
int main()
{
    int t,sum;
    char a[10000];
    scanf("%d",&t);
    getchar();
    while(t--)
    {
        sum=0;
        gets(a);
        sum+=a[1]+a[3]+a[6]+a[8]+a[11]+a[13]+a[16]+a[18]-(48*8);
        sum+=((a[0]-'0')*2)%10+((a[0]-'0')*2)/10+((a[2]-'0')*2)%10+((a[2]-'0')*2)/10+((a[5]-'0')*2)%10+((a[5]-'0')*2)/10+((a[7]-'0')*2)%10+((a[7]-'0')*2)/10+((a[10]-'0')*2)%10+((a[10]-'0')*2)/10+((a[12]-'0')*2)%10+((a[12]-'0')*2)/10+((a[15]-'0')*2)%10+((a[15]-'0')*2)/10+((a[17]-'0')*2)%10+((a[17]-'0')*2)/10;
            puts(sum%10? "Invalid":"Valid");
    }
    return 0;
}
