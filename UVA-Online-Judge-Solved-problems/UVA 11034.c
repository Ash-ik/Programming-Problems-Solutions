#include<stdio.h>
int main()
{
    int t;
    scanf("%d",&t);
    while(t--)
    {
        int l,m,sum=0,ans=0;
        scanf("%d %d",&l,&m);
        l*=100;
        for(i=0;i<<m;i++)
        {
            int x;
            char *side;
            scanf("%d %s",&x,&side);
            if(strcmp(side,))
            if(sum+x>l) {
                    ans++;
            }
            getchar();

        }

    }
}
